#!/usr/bin/env python3
"""Contains the function concat"""


def concat(str1: str, str2: str) -> str:
    """Concatenates str1 and str2 and returns
    the concatenated string
    """
    return '{}{}'.format(str1, str2)
